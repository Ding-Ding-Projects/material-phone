# Handoff

## Current lane

This branch adds the first substantial Material Phone documentation website without modifying desktop source, root build scripts, workflows, submodules, or packaging paths.

### Implemented

- Static HTML/CSS/TypeScript website under `site/`.
- Responsive left-docked ARIA tab navigation.
- Local theme, language, and independent funny-level persistence.
- Settings search with adjacent bounded JavaScript regex builder.
- `Ctrl+Shift+F` command palette and exact-control navigation.
- Desktop/web feature inventory with 64 explicit entries and honest status semantics.
- Status cards, disabled download state, local documentation index, and visible runtime boundary copy.
- Categorized feature documentation and public project records.

### Evidence boundary

- Deterministic structural checks run locally through `node --test site/test-site.mjs`.
- No browser capture or built desktop interaction is claimed.
- Status Hub publishing was unavailable in this lane.
- No verified Material Phone installer or immutable release manifest exists, so the download action remains disabled.
- Open Graph text metadata exists, but a product-specific image and deployed absolute URL remain open.

### Next owner actions

1. Run the approved hidden browser interaction route against a locally served site at phone, tablet, and desktop widths.
2. Record real captures and fix any keyboard, clipping, contrast, or mobile interaction defects.
3. Publish a verified release manifest before enabling any installer link.
4. Implement non-excluded inventory rows in bounded batches, keeping status labels evidence-driven.
